import { StyleSheet } from "react-native";

export default StyleSheet.create({
  sheetView: {
    paddingHorizontal: 16,
    paddingBottom: 32,
    gap: 12,
  },
  sheetViewNoGap: {
    paddingHorizontal: 16,
    paddingBottom: 32,
  },
});
